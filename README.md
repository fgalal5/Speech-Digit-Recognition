# Speech-Digit-Recognition
This work studies noise-robust spoken digit recognition under a fixed-model constraint: I improve the input representation while keeping the baseline bidirectional LSTM classifier unchanged.
